const express = require("express");
// const { createRole } =   require("../controllers/roleControllers"); // Remove .js extension

const route_role = express.Router();

// route_role.post('/create', createRole);

module.exports = route_role;
